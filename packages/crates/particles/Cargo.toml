[package]
name = "awsm-renderer-particles"
description = "Pure CPU particle simulator (struct-of-arrays, GPU-shape compatible)"
edition.workspace = true
version.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
repository.workspace = true

[features]
default = []
serde = ["dep:serde", "awsm-renderer-curves/serde"]

[dependencies]
awsm-renderer-curves = { workspace = true }
awsm-renderer-geometry = { workspace = true }
glam = {workspace = true}
serde = {workspace = true, optional = true}
